eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false #no register for yourself
    fetchRegistry: false

    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}/${server.port}/eureka/
server:
  port: 8761